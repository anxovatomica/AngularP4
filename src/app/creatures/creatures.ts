export class Creatures {
    name: string = '';
    birth: string = '';
    device: string = '';
    imagen: string = '';

    constructor(name: string, birth: string, device: string, imagen: string) {
        this.name = name;
        this.birth = birth;
        this.device = device;
        this.imagen = imagen;
    }
}